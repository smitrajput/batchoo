import * as ethers from 'ethers';
export default class AuthereumSigner extends ethers.Signer {
    private authereum;
    private config;
    provider: any;
    constructor(config: any);
    getAddress(): Promise<string>;
    signMessage(message: string): Promise<string>;
    private sign;
    sendTransaction(transaction: ethers.providers.TransactionRequest): Promise<ethers.providers.TransactionResponse>;
    sendTransactionBatch(transactions: ethers.providers.TransactionRequest[]): Promise<ethers.providers.TransactionResponse>;
    estimateGasBatch(transactions: ethers.providers.TransactionRequest[]): Promise<number[]>;
}
